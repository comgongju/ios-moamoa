# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'ios-moamoa' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-moamoa
  pod 'Alamofire', '~> 5.2'

  target 'ios-moamoaTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
